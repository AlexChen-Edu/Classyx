{
  "name": "classyx-landing",
  "private": true,
  "version": "0.1.0",
  "description": "Marketing landing page + email waitlist for Classyx — an AI study app with a parent accountability dashboard.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  },
  "engines": {
    "node": ">=18"
  }
}
